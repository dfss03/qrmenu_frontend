import { Button } from 'react-bootstrap';
import styled from 'styled-components';

const BotonDeOperacion = styled(Button)`
    width: 30px;
    height: 30px;
    margin: 0 10px;
    font-size: 20px;
    line-height: 18px;
`;

export default BotonDeOperacion;